apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: azure-servicebus-auth
  namespace: hello-chris
spec:
  secretTargetRef:
    - key: connection-string
      name: connection-string # name of secret
      parameter: connection-string # key in secret